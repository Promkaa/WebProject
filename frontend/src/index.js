import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'
import MainStr from './main';

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(<MainStr />);
